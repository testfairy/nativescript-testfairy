<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded" class="page"
  xmlns:ui="nativescript-testfairy">
  <ListView items="{{ myItems }}" itemTap="{{ onTap }}">
	<ListView.itemTemplate>
       <Label text="{{ name }}" class="title" />
    </ListView.itemTemplate>
  </ListView>
</Page>